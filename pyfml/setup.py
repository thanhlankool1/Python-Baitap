from setuptools import setup

setup(
    name='pyfml',
    packages=['exercises'],
    include_package_data=True,
    install_requires=[
    ],
    setup_requires=[
    ],
    tests_require=[
    ],
)
